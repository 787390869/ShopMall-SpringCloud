<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="qa.Dao.Mapper">
<select id="getQuestionById" resultType="qa.Bean.Question">
    select * from question where id = #{id}
</select>
    <resultMap id="EmployeeDetail" type="qa.Bean.Employee">
        <id property="empId" column="EMP_ID"/>
        <result property="empName" column="EMP_NAME"/>
        <association property="department" javaType="qa.Bean.Department">
            <id property="deptId" column="DEP_ID"/>
            <result property="deptName" column="DEPT_NAME"/>
        </association>
    </resultMap>
    <select id="getEmployee" resultMap="EmployeeDetail">
        select * from employee left join department on employee.DEPT_ID = department.DEPT_ID
    </select>
</mapper>
